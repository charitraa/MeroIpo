# Copy to `.env` and adjust. This file is committed; `.env` is the active config.
MEROSHARE_BASE_URL=https://meroshare.cdsc.com.np/api/v2
BACKEND_BASE_URL=
DEFAULT_APPLY_QUANTITY=10
BACKGROUND_POLL_INTERVAL_MINUTES=120
